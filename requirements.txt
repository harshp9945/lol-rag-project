scikit-learn>=1.3
numpy>=1.24
pytest>=7.0
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
# optional extras for the embeddings retriever:
#   pip install sentence-transformers chromadb
# optional extras for real answer generation (free, local):
#   install Ollama from https://ollama.com, then: ollama pull llama3.1
