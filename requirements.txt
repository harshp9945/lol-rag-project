scikit-learn>=1.3
numpy>=1.24
pytest>=7.0
# optional extras for the embeddings retriever:
#   pip install sentence-transformers chromadb
# optional extras for the Anthropic backend:
#   pip install anthropic
